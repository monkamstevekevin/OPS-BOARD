server:
  port: 8060

spring:
  flyway:
    enabled: false
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/opsdb}
    username: ${DB_USER:ops}
    password: ${DB_PASS:ops}
  jpa:
    hibernate:
      ddl-auto: update   # simple v1 (on passera � Flyway ensuite)
    open-in-view: false  # bonne pratique JPA
    properties:
      hibernate.jdbc.time_zone: UTC
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  api-key: ${INGEST_API_KEY:CHANGEMOI123}
  status:
    upMinutes: 5
    staleMinutes: 10
proxmox:
  base-url: https://192.168.1.86:8006/api2/json
  token-id: root@pam!opsboard
  token-secret: ${PVE_TOKEN_SECRET:40838c09-7673-422d-9370-3114ee613c1f}
  default-wait: 10s
admin:
  api-key: CHANGEMOI123
  allow-ips: 127.0.0.1,::1,192.168.1.0/24
